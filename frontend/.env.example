# Opcional en desarrollo (por defecto el cliente usa http://localhost:4000).
# En producción: déjalo vacío si la API va en el mismo dominio (proxy inverso),
# o define la URL pública del API (p. ej. https://api.tudominio.com).
# VITE_API_URL=http://localhost:4000
